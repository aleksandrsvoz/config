## Door Way
type: "custom:button-card"
template:
  - card_room
name: Парадная
entity: lock.front_dore
icon: mdi:door-closed
styles:
  icon:
    # Icon color
    - color: "#AB2E00"
  card:
    # Click Ripple color
    - --mdc-ripple-color: "#AB2E00"
    - --mdc-ripple-press-opacity: 0.3
    # Card bacground color
    - background: "#FDE6E1"
    # Card text color
    - color: "#AB2E00"
  img_cell:
    # Card icon circle color
    - background-color: "#FFC6AC"
tap_action:
  action: navigate
  navigation_path: "/ui-lovelace-minimalist/bathroom"
variables:
  label_use_temperature: false
  label_use_brightness: true
  entity_1:
    entity_id: lock.front_dore
    tap_action:
      action: toggle
custom_fields:
  i1:
    card:
      type: "custom:button-card"
      template: >
        [[[
          let templates = [ 'widget_icon_room' ];
          if (variables?.entity_1?.templates?.length) {
            templates.push(...variables.entity_1.templates);
          }
          return templates;
        ]]]
      variables: "[[[ return variables?.entity_1; ]]]"
      state:
        - operator: "template"
          value: "[[[ return !variables.entity_1; ]]]"
          styles:
            card:
              - display: "none"

        - value: "unlocked"
          icon: "[[[ return states['binary_sensor.front_dore_door'].state === 'on' ? 'mdi:door-open' : 'mdi:lock-open-variant'; ]]]"
          styles:
            card:
              - background-color: "#FFA4A4"
            icon:
              - color: "#B60021"

        - value: "locked"
          icon: "[[[ return states['binary_sensor.front_dore_door'].state === 'on' ? 'mdi:lock-alert' : 'mdi:lock'; ]]]"
          styles:
            card:
              - background-color: "[[[ return states['binary_sensor.front_dore_door'].state === 'on' ? '#FFA4A4' : '#FFC6AC'; ]]]"
            icon:
              - color: "[[[ return states['binary_sensor.front_dore_door'].state === 'on' ? '#B60021' : '#AB2E00'; ]]]"

        - value: "unlocking"
          icon: mdi:lock-clock
          styles:
            card:
              - background-color: "#F8CDB7"
            icon:
              - color: "#DA5C22"

        - value: "locking"
          icon: mdi:lock-clock
          styles:
            card:
              - background-color: "#F8CDB7"
            icon:
              - color: "#DA5C22"
      entity: "[[[ return variables?.entity_1?.entity_id; ]]]"
